version: '3.7'

services:
    blackrock_gunicorn:
        build:
          context: .

        volumes:
            # - .:/app/
            - staticfiles:/app/staticfiles
        
        container_name: blackrock_gunicorn
        
        ports:
            - "8000:8000"
        # env_file: 
        #     - './tsm_auth/.env'

        networks:
            - blackrock_network
        restart: always

    nginx:
        build: ./nginx
        volumes: 
            - staticfiles:/app/staticfiles
            # - /etc/letsencrypt/csr/:/etc/letsencrypt/csr/
            # - /etc/letsencrypt/keys/:/etc/letsencrypt/keys/

        ports: 
            - "80:80"
            # - "443:443"
        networks:
            - blackrock_network
        
        depends_on: 
            - blackrock_gunicorn
    
volumes:
    staticfiles:
    blackrock_authservice: 

networks:
    blackrock_network:
        driver: bridge

